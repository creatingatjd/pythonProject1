# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

name: $(Rev:r)
pool:
  vmImage: ubuntu-latest
stages:
- stage: build
  jobs:
    - job: build
      steps:
      - task: Trivy@1
        inputs:
          image: 1234si/pythonapp
          severity: HIGH,CRITICAL
          outputFormat: JUnit
